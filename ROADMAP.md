# ROADMAP — Польовий Модуль

## 0. Поточна стабільна база

**Стабільна база перед цим checkpoint:** `V19.17.12 — Journal Legacy Cleanup Checkpoint`, build/cache `19629`.

**Поточний checkpoint:** `V19.17.13 — ROADMAP Reorder + Combat Architecture Plan`, build/cache `19630`.

V19.17.13 не змінює бойову математику. Його задача — впорядкувати roadmap, зафіксувати locked rules і спланувати наступні архітектурні кроки.

### Підтверджено стабільним у V19.17.12

- 1 патрон: без розкриття.
- 2 патрони: розкриття `-1 Захист` тільки якщо `0` влучань.
- 3 патрони / черга: розкриття `-2 Захист` після кожної черги.
- Віддача черги прогресує `-2 / -4 / -6 / -8...`.
- Постріл 1 або 2 патронами скидає віддачу черги.
- Ціль в укритті: `+2` до ефективного Захисту цілі.
- Черга по цілі в укритті: додатково `-1` до точності стрільця.
- Гравець бачить Захист цілі та пояснення модифікаторів Захисту.
- Гравець не бачить точні HP ворогів.
- Майстер бачить усе.
- Старий технічний бойовий запис у журналі прибрано/відфільтровано.

---

## 1. Locked Combat Rules

Ці правила не змінювати без прямого дозволу користувача.

### Стрільба гравця / людини зі зброєю

1. **1 патрон — точний постріл**
   - `1d20 +2`;
   - витрачає 1 набій;
   - 1 влучання = повна шкода зброї;
   - розкриття немає.

2. **2 патрони — бойовий постріл**
   - `2d20 -1`;
   - витрачає 2 набої;
   - 1-е влучання = повна шкода;
   - 2-е влучання = тільки кубик шкоди без повторного плоского бонусу;
   - якщо `0` влучань: стрілець розкрився, `Захист -1` до наступного ходу;
   - якщо є хоча б 1 влучання: розкриття немає.

3. **3 патрони — черга**
   - `3d20`;
   - штраф віддачі: `-2 / -4 / -6 / -8...` за черги поспіль;
   - 1 або 2 патрони скидають віддачу;
   - 1-е влучання = повна шкода;
   - додаткові влучання = тільки кубик шкоди;
   - після кожної черги стрілець розкрився, `Захист -2` до наступного ходу.

### Укриття

- Ціль в укритті: `+2` до ефективного Захисту цілі.
- Черга по цілі в укритті: додатково `-1` до точності стрільця.

### Видимість

- Гравець бачить Захист цілі.
- Гравець бачить пояснення, чому Захист цілі змінений.
- Гравець не бачить точні HP ворога.
- Майстер бачить HP, набої, мораль, лут, приховані деталі, технічні дані.

---

## 2. Technical Debt / Architecture Health

Оцінка після V19.17.11–V19.17.12:

- `app.js` ≈ 5088 рядків;
- `styles.css` ≈ 2189 рядків;
- `index.html` ≈ 375 рядків;
- разом із документацією ≈ 9845 рядків.

Комфорт роботи:

- загальний комфорт ≈ 72%;
- `app.js` ≈ 62–65%;
- CSS ≈ 75%;
- HTML ≈ 85%;
- документація ≈ 80%.

Практичний запас `app.js`:

- до 6000 рядків — ще нормально, але обережно;
- 6000–7500 рядків — жовта зона, потрібна чистка й модульність;
- 7500+ рядків — бажано архітектурне розбиття.

Головний ризик — не кількість рядків сама по собі, а дублювання логіки:

- бойовий підсумок;
- toast;
- журнал;
- role-aware privacy;
- старі функції бойового тексту;
- UI-форматування одних і тих самих даних у різних місцях.

---

## 3. Найближчі технічні задачі

### V19.17.14 або V19.18 — Combat Result Object

Ціль: один об’єкт результату атаки, з якого будуються всі відображення.

`combatResult` має містити:

- хто атакував;
- кого атакував;
- тип атаки / режим стрільби;
- сирі d20;
- фінальні значення після модифікаторів;
- список модифікаторів точності;
- ефективний Захист цілі;
- пояснення модифікаторів Захисту;
- кількість влучань;
- кубики шкоди;
- загальну шкоду;
- HP цілі для GM;
- публічний стан цілі для гравця;
- залишок набоїв;
- наслідки: розкриття, клин, крит, укриття, віддача.

З цього об’єкта мають будуватися:

- `Остання дія`;
- toast;
- журнал;
- GM-версія;
- player-версія.

### V19.17.15 — Role-aware Visibility Filters

Окремий шар видимості:

- `gmCombatView(result)`;
- `playerCombatView(result)`;
- `enemyVisibleStateForRole(enemy, role)`;
- `targetDefenseViewForRole(target, role)`.

Мета: не шукати HP ворога через `.replace()`, а мати контрольований фільтр.

### V19.17.16 — Combat Helper Sections

Без фізичного розбиття файлів, але з чіткими секціями в `app.js`:

- `COMBAT: constants`;
- `COMBAT: attack resolution`;
- `COMBAT: damage`;
- `COMBAT: recoil / exposure`;
- `COMBAT: summary rendering`;
- `COMBAT: journal / privacy`;
- `COMBAT: UI integration`.

### Stable Combat Checkpoint

Після 2–3 cleanup-версій зафіксувати стабільний бойовий checkpoint.

---

## 4. Найближчі ігрові задачі після cleanup

1. Ще 1–2 шаблони ворогів:
   - звичайний бандит;
   - панікер / засадник;
   - слабкий мутант як перший тест не-людського ворога.

2. Command Core Майстра:
   - швидка подія;
   - швидке ускладнення;
   - швидкий лут;
   - швидке повідомлення гравцю;
   - швидка зміна видимості.

3. Тестовий режим бою:
   - швидко перевірити 1/2/3 патрони;
   - скинути віддачу;
   - скинути розкриття;
   - поставити ворога в укриття / прибрати укриття;
   - симулювати 0/1/2/3 влучання для тестів тексту.

---

## 5. Режими видимості / “можливість усього”

Майбутній принцип: більше функціональності і можливість усього.

Потрібні режими:

1. **Чесний тест**
   - максимум цифр;
   - всі модифікатори;
   - точний Захист;
   - розбір формул;
   - зручно для балансу й тестування.

2. **Атмосферна гра**
   - гравець може бачити не все;
   - Захист може бути описом: `легко влучити`, `важко влучити`, `майже неможливо`;
   - частина модифікаторів може бути прихована.

3. **Кастомний режим Майстра**
   - Майстер сам обирає, що бачить гравець:
     - точний Захист;
     - модифікатори Захисту;
     - укриття;
     - точність;
     - стан ворога;
     - HP ворога;
     - небезпека;
     - лут;
     - мораль.

Поточне правило для тестової гілки: гравець бачить Захист цілі та пояснення модифікаторів, але не бачить HP ворога.

---

## 6. Середній горизонт

Після стабілізації бойового фундаменту:

- квести / задачі;
- журнал як КПК із вкладками;
- інвентар із типами предметів;
- лут як структуровані предмети;
- сцени з прихованими деталями;
- NPC;
- розширені шаблони ворогів.

---

## 7. Великий горизонт

- аномалії;
- артефакти;
- броня / костюми;
- кампанії;
- імпорт / експорт;
- бібліотеки шаблонів;
- історія предметів;
- ШІ-помічник Майстра;
- ШІ-помічник гравця;
- віртуальні гравці для тестів;
- голосові команди;
- онлайн-платформа.

---

## 8. Принцип роботи над версіями

- Маленькі версії.
- Не змінювати бойову математику без прямого дозволу.
- Завжди оновлювати cache-busting.
- Завжди давати ZIP.
- Завжди давати тестові посилання Майстер/Гравець.
- Якщо користувач каже, що все працює, версію можна вважати стабільною.
- Перед великими змінами спершу аналізувати ризики.
